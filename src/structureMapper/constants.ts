const PATH_IDENTIFIER = '@';
const ARR_ELEM = '$elem';
const ELEM_PARENT = '$parent';
const PUSH = 'push';
const ATTRIBUTE_VALUE = 'value';
const CONCAT = 'concat';
const CONDITIONS = 'conditions';
const LOOP = 'for';
const ARRAY_IDENTIFIER = '$array';
const SET_ATTRIBUTE_VALUE = 'push';

const BIN_OPS = ['or', 'and', 'gt', 'lt', 'equal', 'notEqual'];
const UNARY_OPS = ['notExist', 'exist'];

export {
  PATH_IDENTIFIER,
  PUSH,
  ATTRIBUTE_VALUE,
  CONCAT,
  CONDITIONS,
  LOOP,
  BIN_OPS,
  UNARY_OPS,
  ARRAY_IDENTIFIER,
  SET_ATTRIBUTE_VALUE,
  ARR_ELEM,
  ELEM_PARENT
};
